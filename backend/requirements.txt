# FastAPI healthcare AI backend — production dependencies
# Install PyTorch CPU separately in Dockerfile for smaller images.

# API
fastapi==0.136.1
uvicorn[standard]==0.47.0
starlette==1.0.0
python-multipart==0.0.29

# Auth
python-jose[cryptography]==3.5.0
passlib[bcrypt]==1.7.4
bcrypt==5.0.0

# Database
sqlalchemy==2.0.49
psycopg2-binary==2.9.11

# Configuration
python-dotenv==1.2.1

# ML stack (predictor, SHAP explainability)
numpy==2.4.6
pandas==3.0.3
scikit-learn==1.8.0
joblib==1.5.3
shap==0.51.0

# SHAP / explainability support
numba==0.65.1
cloudpickle==3.1.2
slicer==0.0.8
tqdm==4.67.3

# Optional report generation (shap_explainer CLI)
matplotlib==3.10.9
torch